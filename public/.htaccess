<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  
  # API Proxy bypass (lets Passenger/Node handle it)
  RewriteCond %{REQUEST_URI} !^/api/ [NC]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
